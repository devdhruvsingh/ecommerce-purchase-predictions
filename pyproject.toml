[build-system]
requires = ["setuptools>=61"]
build-backend = "setuptools.build_meta"

[project]
name = "ecommerce-purchase-predictions"
version = "0.1.0"
description = "E-commerce customer purchase prediction machine learning project"

[tool.setuptools.packages.find]
where = ["."]
include = ["src*"]
